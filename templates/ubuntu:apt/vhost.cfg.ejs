{host_config, "<%= host %>" ,[
  <% admins.forEach(function(admin, index) { %>{acl, admin, {user, "<%= admin %>", "<%= host %>"}},<% }); %>
  {access, muc_admin, [{allow, admin}, {deny, all}]},
  {{add, modules}, [
    {mod_muc, [
    %%{host, "conference.@HOST@"},
      {access, all},
      {access_admin, muc_admin},
      {access_create, muc_admin},
      {access_persistent, all}
    ]}
  ]}
]}.
